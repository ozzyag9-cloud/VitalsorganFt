# Zero-knowledge workspace

This directory reserves architecture for future privacy proofs:

- `semaphore/` for private group membership and nullifier proofs.
- `circom/` for Circom circuits and verifier artifacts.
- `noir/` for Noir programs.
- `snarks/` for shared proving keys, verification keys, and generated Solidity verifiers.
